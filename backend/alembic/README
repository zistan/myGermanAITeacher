Generic single-database configuration for German Learning App.

To create a new migration:
    alembic revision --autogenerate -m "description of changes"

To apply migrations:
    alembic upgrade head

To revert last migration:
    alembic downgrade -1

To view migration history:
    alembic history

To view current revision:
    alembic current
